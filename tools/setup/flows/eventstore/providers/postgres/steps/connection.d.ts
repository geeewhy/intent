/**
 * Set up database connection
 */
import { FlowCtx } from '../../../../../shared/types';
/**
 * Set up database connection
 * @param ctx Flow context
 */
export default function step(ctx: FlowCtx): Promise<void>;
